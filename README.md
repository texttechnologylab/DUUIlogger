# duui-logging

Logging library for DUUI Python tool components. It lets a tool emit structured info / warning / error logs during a request and have them surfaced on the Java side (printed to the Java console and saved in a connected database).

